@model notomyk.ViewModel.RightMenuForum
@using notomyk.Models

<div class="rightTable col-xs-12">
    <ul class=" nav nav-pills">
        @*<li class="active">
                <a href="#1a" data-toggle="tab"><small><stron>Informacje</stron></small></a>
            </li>
            <li>
                <a href="#2a" data-toggle="tab"><small><strong>Popularne</strong></small></a>
            </li>
            <li>
                <a href="#3a" data-toggle="tab"><small><strong>Uwagi</strong></small></a>
            </li>*@
    </ul>

    <div class="tab-content clearfix">
        <div class="tab-pane active" id="1a">
            <div class="rightList">

                @foreach (var topic in Model.Topics)
                {
                    var imgUrl = Url.Content(AppConfig.UserLogoLink(topic.ApplicationUser.Id));
                    <div class="post">
                        <a href="@Url.Action("Topic", "Forum", new {ID = @topic.ID})">
                            <div class="post-heading">
                                <div class="pull-left">
                                    <img src="@imgUrl" class="avatar NTMavatar" alt="user profile image">
                                </div>
                                <div class="pull-left meta">
                                    <div class="title h5">
                                        <div style="height:17px">
                                            <small class="rightMenuPostTitle"><strong>@topic.Subject</strong></small>
                                        </div>
                                        <div style="height:17px">
                                            <small class="text-muted">Dodano: @GetTimeAgo.CalculateDateDiff(topic.DateAdd)</small>
                                        </div>
                                    </div>
                                </div>
                                <div class="pull-right meta">
                                    <div>
                                        <span class="ui blue basic label">1520</span>
                                    </div>
                                </div>
                            </div>
                        </a>
                    </div>
                }
            </div>
        </div>
    </div>
</div>
